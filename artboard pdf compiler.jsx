#target "Illustrator"var doc = app.activeDocument;var boards = doc.artboards;var sels = doc.pageItems;var saveFolder = Folder.selectDialog("アートボード毎にPdfを保存します。保存先のフォルダを選択してください") ;var pdfOption = new PDFSaveOptions();pdfOption.compatibility = PDFCompatibility.ACROBAT8;pdfOption.preserveEditability = false;pdfOption.generateThumbnails = true;pdfOption.optimization = false;pdfOption.viewAfterSaving = false;pdfOption.acrobatLayers = false;for (i = 0; i < boards.length ; i ++) {    var saveFile = new File(saveFolder+'/'+boards[i].name + ".ai");    pdfOption.artboardRange = i+1;    doc.saveAs(saveFile, pdfOption);};